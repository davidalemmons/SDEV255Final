<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #0b6623;">   
<!--desktop display-->

    <div class="container-fluid" id="desktop">
        <a class="navbar-brand d-flex align-items-center" href="/">
            <img src="/images/IvyTechLogo.png" alt="Ivy Tech Logo" width="50" height="50" class="me-2">
            <span>Ivy Tech Community College</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <div class="d-flex w-100 justify-content-end align-items-center">
                <form class="d-flex me-3" action="/courses/search" method="GET">
                    <input class="form-control me-2" type="search" name="query" placeholder="Search courses..." aria-label="Search">
                    <button class="btn btn-dark" type="submit">Search</button>
                </form>
                <a href="/courses" class="btn btn-dark me-2">View All Courses</a>
                <% if (user && user.role === 'teacher') { %>
                    <a href="/courses/new" class="btn btn-dark">Add Course</a>
                <% } %>
                <% if (user && user.role === 'student') { %>
                    <a href="/schedule" class="btn btn-dark">Schedule</a>
                <% } %>
                <% if (user) { %>
                    <a href="/profile" class="btn btn-dark me-2">Profile</a>
                    <form action="/logout" method="GET" style="display: inline;">
                        <button class="btn btn-danger">Logout</button>
                    </form>
                <% } else { %>
                    <a href="/login" class="btn btn-dark me-2">Login</a>
                    <a href="/register" class="btn btn-dark">Register</a>
                <% } %>
                
            </div>
        </div>
    </div>

    <div class="container-fluid g-0" id="mobile">
        <a class="navbar-brand d-flex align-items-center logo" href="/">
            <img src="/images/IvyTechLogo.png" alt="Ivy Tech Logo" width="50" height="50" class="me-2">
            <span>Ivy Tech Community College</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <div class="d-flex w-100 justify-content-end align-items-center flex-parent">
                <form class="d-flex me-3 search" action="/courses/search" method="GET">
                    <input class="form-control me-2" type="search" name="query" placeholder="Search courses..." aria-label="Search">
                    <button class="btn btn-dark" type="submit">Search</button>
                </form>
                <a href="/courses" class="nav-mobile bg-dark me-2">View All Courses</a>
                <% if (user && user.role === 'teacher') { %>
                    <a href="/courses/new" class="nav-mobile bg-dark">Add Course</a>
                <% } %>
                <% if (user && user.role === 'student') { %>
                    <a href="/schedule" class="nav-mobile bg-dark">Schedule</a>
                <% } %>
                <% if (user) { %>
                    <a href="/profile" class="nav-mobile bg-dark me-2">Profile</a>
                    <form action="/logout" method="GET" style="display: inline;">
                        <button class="logout nav-mobile bg-danger">Logout</button>
                    </form>
                <% } else { %>
                    <a href="/login" class="nav-mobile bg-dark me-2">Login</a>
                    <a href="/register" class="nav-mobile bg-dark">Register</a>
                <% } %>
                
            </div>
        </div>
    </div>
</nav>


